# Medium Rosetta config for GPU training
# ~30M parameters, suitable for single GPU

d_model: 512
n_heads: 8
n_layers: 12
n_frame_layers: 4
d_ff: 2048
max_seq_len: 4096
batch_size: 16
gradient_accumulation_steps: 4
learning_rate: 0.0001
warmup_steps: 4000
max_steps: 200000
